// Package config provides the configuration for the application. Note that most
// configuration is provided by the underlying trueblocks-core packages, so the
// items here are mostly for the application itself.
package config
